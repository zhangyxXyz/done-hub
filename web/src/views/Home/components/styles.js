// Type system for the landing page only (not the global theme). Inter is already
// bundled via @fontsource-variable/inter; here it becomes the display face, with
// a monospace for the editorial labels — the grotesk + mono pairing that reads as
// a high-end developer product rather than an admin dashboard.
export const DISPLAY = '"Inter Variable", -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif';
export const MONO = '"SFMono-Regular", Consolas, "Liberation Mono", Menlo, monospace';
